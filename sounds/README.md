# sounds —— 提示音目录

程序会在此目录读取两个音频文件（`.ogg`）：

| 文件 | 用途 |
|---|---|
| `Spring.ogg` | **系统提示音**：自检异常等系统级提醒 |
| `Bubble.ogg` | **消息提示音**：收到买家新消息 |

- 仓库**不附带**音频文件（避免版权问题），请自行放入同名文件；
- 缺失时只是不播放声音，不影响其它功能；
- 想用自己的音效：把任意 `.ogg` 文件按上面命名放入即可；也可用环境变量
  `XY_SOUND_SYS` / `XY_SOUND_MSG` 指定任意路径。
